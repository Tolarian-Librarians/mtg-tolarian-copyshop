<simpleChildWindow:ChildWindow x:Class="Tolarian.Copyshop.ScreenPresenter.Views.ImportCardsChildView"
                               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                               xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                               xmlns:iconPacks="http://metro.mahapps.com/winfx/xaml/iconpacks"
                               xmlns:local="clr-namespace:Tolarian.Copyshop.ScreenPresenter.Views"
                               xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                               xmlns:simpleChildWindow="clr-namespace:MahApps.Metro.SimpleChildWindow;assembly=MahApps.Metro.SimpleChildWindow"
                               Title="Import Cards"
                               Width="800"
                               Height="450"
                               GlowBrush="{DynamicResource MahApps.Brushes.Accent}"
                               ShowCloseButton="True"
                               mc:Ignorable="d">

    <simpleChildWindow:ChildWindow.Icon>
        <iconPacks:PackIconFontAwesome Width="22"
                                       Height="22"
                                       HorizontalAlignment="Center"
                                       VerticalAlignment="Center"
                                       Kind="AngleDoubleRightSolid" />
    </simpleChildWindow:ChildWindow.Icon>

    <simpleChildWindow:ChildWindow.OverlayBrush>
        <SolidColorBrush Opacity="0.7" Color="{DynamicResource MahApps.Colors.Black}" />
    </simpleChildWindow:ChildWindow.OverlayBrush>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <TextBox Grid.Row="0"
                 Grid.ColumnSpan="2"
                 MinWidth="400"
                 MinHeight="200"
                 Margin="30,15,30,15"
                 AcceptsReturn="True"
                 AcceptsTab="False"
                 Text="{Binding ImportCard}"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Visible" />
        <Button Grid.Row="1"
                Grid.Column="0"
                Margin="30,0,30,10"
                Command="{Binding AffirmativeCommand}"
                Content="OK" />
        <Button Grid.Row="1"
                Grid.Column="1"
                Margin="30,0,30,10"
                Command="{Binding NegativeCommand}"
                Content="Cancel" />
    </Grid>
</simpleChildWindow:ChildWindow>